{
  "name": "rebalance",
  "version": "1.0.0",
  "description": "Portfolio exposure tracker — E2E tests",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
